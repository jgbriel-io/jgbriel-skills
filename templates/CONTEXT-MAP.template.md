# Context Map

## Contexts

- [{Context Name}](./src/{path}/CONTEXT.md) — {one-line description of what this context owns}
- [{Context Name}](./src/{path}/CONTEXT.md) — {one-line description}

## Relationships

- **{Context A} → {Context B}**: {Context A} emits `{EventName}` events; {Context B} consumes them to {action}
- **{Context A} ↔ {Context B}**: Shared types for `{TypeName}`
